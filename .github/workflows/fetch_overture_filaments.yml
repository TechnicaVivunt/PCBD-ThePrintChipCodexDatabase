name: Fetch Overture Filaments

on:
  schedule:
    - cron: "0 9 * * *"  # Runs every day at 9 AM UTC
  workflow_dispatch:

jobs:
  fetch_and_notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests jq

      - name: Run filament script
        id: fetch
        run: |
          output=$(python dbworker/fetch_overture_filaments.py)
          echo "$output" > output.json
          ROWS=$(echo "$output" | jq '.rows')
          CHANGED=$(echo "$output" | jq '.changed')
          echo "rows=$ROWS" >> $GITHUB_OUTPUT
          echo "changed=$CHANGED" >> $GITHUB_OUTPUT
          
      - name: Commit updated CSV if changed
        if: steps.fetch.outputs.changed == 'true'
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"
          git add dbworker/overture_filament_index.csv dbworker/overture_filament_index.hash
          git commit -m "Update Overture filament CSV [ci skip]" || echo "No changes to commit"
          git push origin HEAD:${{ github.ref }}

      - name: Notify Discord if changed
        if: steps.fetch.outputs.changed == 'true'
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          username: "Overture Scraper"
          description: |
            **Overture Filament Update**
            CSV updated in `dbworker/overture_filament_index.csv`.
            Total variants fetched: ${{ steps.fetch.outputs.rows }}.
            Check the repo for details: https://github.com/${{ github.repository }}
          color: 3066993
